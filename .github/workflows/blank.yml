name: Second workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
          echo "Hello world!"
          echo "hello world again!"
      - name: Print goodbye
        run: echo "Done - bye!"
